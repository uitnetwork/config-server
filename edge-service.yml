server:
  port: 8989
zuul:
  routes:
    resouces1:
      path: /resources1/**
      serviceId: resource-service
      # serviceId: resource-service
      sensitiveHeaders: Cookie,Set-Cookie
  debug:
    request: true